# Vercel 部署时在 Vercel 项目设置 -> Environment Variables 中配置（不带末尾斜杠）
# 例如：http://8.163.87.4:8080
# 由 api/[...path].js Edge Function 在服务端代理转发，解决 Mixed Content 问题
# Docker 部署时不需要此变量，由 nginx 代理处理
BACKEND_URL=

